[package]
name = "bad-password"
version = "0.1.0"
edition = "2021"
description = "A satirical weak password generator"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
rand = "0.8"
chrono = "0.4"

[profile.release]
lto = true
strip = true
